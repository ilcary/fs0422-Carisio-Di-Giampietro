import { UpperTitlePipe } from './upper-title.pipe';

describe('UpperTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new UpperTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
